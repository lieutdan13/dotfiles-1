---

vim_packages:
    - { name: exuberant-ctags, default_release: 'wheezy-backports' }
    - { name: cmake, default_release: 'wheezy' }
    - { name: neovim, default_release: 'wheezy' }

vim_files:
    - .vim/colors/jellybeans.vim

vim_templates:
    - .vimrc
    - .ycm_global_conf.py

vim_dirs:
    - bundle
    - colors
