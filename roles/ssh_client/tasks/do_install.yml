---

- name: install packages
  sudo: true
  apt: >
      name={{ item }}
      state=present
      update_cache=true
  tags: packages
  with_items: ssh_client_packages

- name: install templates
  template: >
      src={{ item }}.j2
      dest={{ ssh_client_dot_path }}/{{ item }}
  with_items: ssh_client_templates
